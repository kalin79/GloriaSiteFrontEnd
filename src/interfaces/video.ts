export interface VideoInterface {
    title: string;
    idMarca: number;
    marca: string;
    slug: string;
    imagen: string;
    video: string;
    colorMarca: string;
}

export interface FiltroVideosInterface {
    nombre: string;
    slug: string;
}